using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoiseManager
{
    /*
    [SerializedField] GameObject _quiet;
    [SerializedField] GameObject _loud;
    void Update()
    {
        #region Handles Noises

        if ((isJumping && characterController.isGrounded) || (isRunning && isWalking))
        {
            isJumping = false;
            // Enable loud noise
            _loud.SetActive(true);
        }
        else
        {
            _loud.SetActive(false);
        }
        if (isWalking)
        {
            // Enable quiet noise
            _quiet.SetActive(true);
        }
        else
        {
            _quiet.SetActive(false);
        }

        #endregion
    }
    */
}
